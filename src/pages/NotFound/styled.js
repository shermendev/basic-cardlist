import styled, { css } from 'styled-components'

export const ErrorMessage = styled.p(() => css`
    color: #777;
    font-size: 36px;
  `)

export const RedirectCountDown = styled.div(() => css`
    font-size: 50px;
  `)
