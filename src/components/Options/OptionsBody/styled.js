import styled, { css } from 'styled-components'

export const OptionsBodyContainer = styled.div(() => css`
    left: 100px;
    position: fixed;
    top: 100px;
    z-index: 999;
  `)

export const OptionsBodyOrderComment = styled.span(() => css`
    font-size: 18px;
  `)
